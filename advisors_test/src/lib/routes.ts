const API_baseURL = "http://localhost:3001"
const baseURL = "http://localhost:3000"

export enum API_ROUTES {
  ADVISORS = `${API_baseURL}/advisors`,
}

export enum ROUTES {
  ADVISORS = `${baseURL}/advisors`,
}
